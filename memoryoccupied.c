#include<stdio.h>
void main()
{
    int x;
    float y;
    char z;
    double a;
    printf("\n To check the memory occupied by different data types:");
    printf("\n The memory occupies by int data type=%d\n",sizeof(x));
    printf("\n The memory occupies by float data type=%d\n",sizeof(y));
    printf("\n The memory occupies by char data type=%d\n",sizeof(z));
    printf("\n The memory occupies by double data type=%d\n",sizeof(a));
    printf("\n Hope you understood!");
}
